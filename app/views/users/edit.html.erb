<% provide(:title, t("edit_user") ) %>
<% provide(:button_text, t("helpers.submit.user.save_change")) %>
<h1> <%= t "update_profile" %></h1>

<div class="row">
  <div class="col-md col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t("change"), "https://gravatar.com/emails", target: "_blank", rel: "noopener" %>
    </div>
  </div>
</div>
