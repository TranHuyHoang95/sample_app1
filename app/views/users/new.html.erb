<% provide(:title, t("signup") ) %>
<% provide(:button_text, t("helpers.submit.user.create")) %>
<h1> <%= t "signup" %></h1>

<div class="row">
  <div class="col-md col-md-offset-3">
      <%= render "form" %>
  </div>
</div>
